# TODO Operate on the page object
#page.insert_html :bottom,:partial=>'/kt1/tvc'
#page.replace_html 'bar1', :partial=>'/part/oc'
#
#page.replace_html 'dis',"iiiii"
  id=params[:id]
  update='zakazk_'+ id.to_s
page.replace_html update  , :partial=>'/keitering/tvc' ,:id=>id
#page.replace_html 'bar11', oc(id)
